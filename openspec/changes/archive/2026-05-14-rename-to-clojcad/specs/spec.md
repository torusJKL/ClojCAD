## ADDED Requirements

*None — this change introduces no new capabilities.*

## MODIFIED Requirements

*None — this change modifies no existing spec-level requirements. The rename is purely a namespace and directory change with zero behavioral impact.*
